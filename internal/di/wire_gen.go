// Code generated by Wire. DO NOT EDIT.

//go:generate go run -mod=mod github.com/google/wire/cmd/wire
//go:build !wireinject
// +build !wireinject

package di

import (
	"go_demo/internal/config"
	"go_demo/internal/handler"
	"go_demo/internal/repository"
	"go_demo/internal/router"
	"go_demo/internal/service"
	"go_demo/internal/utils"
	"go_demo/pkg/database"
)

// Injectors from injector.go:

// InitializeContainer 使用 Wire 注入依赖（仅在 wireinject 构建标签下编译）
func InitializeContainer() (*AppContainer, error) {
	configConfig := config.NewConfig()
	jwtConfig := configConfig.JWT
	jwtManager := utils.NewJWTManager(jwtConfig)
	mySQLConfig := configConfig.Database
	db, err := database.NewMySQL(mySQLConfig)
	if err != nil {
		return nil, err
	}
	userRepository := repository.NewUserRepository(db)
	authService := service.NewAuthService(userRepository, jwtManager)
	userService := service.NewUserService(userRepository)
	authHandler := handler.NewAuthHandler(authService, userService, jwtManager)
	userHandler := handler.NewUserHandler(userService)
	routerRouter := router.NewRouter(authHandler, userHandler, jwtManager)
	appContainer := NewAppContainer(configConfig, jwtManager, userRepository, authService, userService, authHandler, userHandler, routerRouter)
	return appContainer, nil
}
